{
  "dependencies": {
    "@nestjs/config": "^4.0.2",
    "axios": "^1.11.0"
  },
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm run build && cp -r ../frontend/build ./dist/public",
    "start": "cd backend && npm run start:prod"
  }
}

